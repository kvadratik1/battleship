class Ship {
  constructor(length) {
    this.length = length;
    this.hits = 0;
    this.sunk = false;
  }

  isSunk() {
    if (length === hits) {
      this.sunk = true;
      return this.sunk;
    } else {
      return this.sunk;
    }
  }
}
